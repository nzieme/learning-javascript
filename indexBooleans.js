//Booleans
let appleNumber = 7
if (appleNumber > 6){
    document.write('congrats!')
} else {document.write('Sorry')
} 
// =(to assign), ==(to compare), ===(value and type).  
// > (greater than), =>(given value is greater than or equal to).
// <= (lower than), <= (lower than or equal to), !=(not equal to).


let appleCount = 20
while(appleCount <= 100) {
    document.write( 'The no of apple is' + appleCount ,"<br>")
appleCount = appleCount + 1
}
//<br> helps to display the output in a single raw each.
// ++ is used to add one to the variable value.
appleCount++
// if we need to add more than 1 (+=5)
appleCount+=5